(() => {

    let person: { name: string, age?: number, cities: string[], getNombre?: () => string  } = {
        name: 'Juan',
        age: 26,
        cities: ['Bogota', 'Mosquera']        
    }
})()